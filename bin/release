#!/bin/sh
echo "-----> Release step"

# Outputting valid YAML for Heroku
cat << EOF
---
default_process_types:
  worker: uvicorn app.main:app --reload --workers 1 --host 0.0.0.0 --port 8000
EOF

exit 0
